apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../vendor/base/workflow-controller

patches:
- path: workflow-controller-deployment-patch.yaml
- path: workflow-controller-configmap-patch.yaml

configurations:
- params.yaml
